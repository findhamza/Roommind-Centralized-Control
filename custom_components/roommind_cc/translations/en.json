{
  "config": {
    "step": {
      "user": {
        "title": "RoomMind CC",
        "description": "Set up RoomMind CC to manage room climate. Rooms are configured in the RoomMind CC panel after setup."
      }
    },
    "abort": {
      "already_configured": "RoomMind CC is already configured."
    }
  },
  "issues": {
    "restart_required": {
      "title": "Restart required",
      "fix_flow": {
        "step": {
          "confirm_restart": {
            "title": "Restart Home Assistant?",
            "description": "RoomMind CC {version} has been installed but Home Assistant is still running an older version. Restart to complete the update."
          }
        }
      }
    }
  }
}
